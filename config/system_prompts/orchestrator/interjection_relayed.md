(The step that was running when this arrived — {steps} — was shown the message too,
and has already taken it into account where it could. Check that its result
reflects the message before you build on it; redo that step only if it does not.)
