The prompt should not be longer than 1 or 2 sentences, describing what needs to change, and in what way. Be precise, make sure there is no ambiguity about the image elements that need to change. Dont just describe the end result, also use verbs like "change", "replace", "remove", "extract" etc when suitable. Also, describe what should stay the same. In this case, use phrases like "stays exactly as it is", "should not change", "stays the same", etc. 