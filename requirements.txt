# Shared tool/util layer (ComfyUI, HuggingFace, web, file, batch). Single source
# of truth, also consumed by the agentY-mcp server. Expects the agenty_core repo
# checked out as a sibling of this one. Install from this repo's root with:
#   uv pip install -e ../agenty_core   (or: pip install -r requirements.txt)
-e ../agenty_core

strands-agents>=0.1.0
strands-agents-tools>=0.3.0
requests>=2.31.0
websockets>=12.0
python-dotenv>=1.0.0
anthropic>=0.49.0
ollama
# OpenAI SDK — used by the Strands OpenAI provider to reach Alibaba Model Studio
# (DashScope)'s OpenAI-compatible endpoint for Qwen models.
openai>=1.0.0
Pillow>=10.0.0
# Chat host + ComfyUI bridge server (src/utils/agentY_server.py) — replaces the
# Chainlit GUI. werkzeug ships with flask; conversations persist to local SQLite
# (stdlib) so no Postgres/boto3/asyncpg/sqlalchemy are needed anymore.
flask>=3.0.0
# Local FAISS memory layer (mem0 + nomic-embed-text via Ollama)
mem0ai>=1.0.0
faiss-cpu>=1.7.0
# spaCy — optional NLP for mem0 (lemmatized BM25 keyword search + entity
# extraction). The memory layer works without it, just in a degraded mode.
# The en_core_web_sm model is pinned by URL so `uv pip install -r requirements.txt`
# gets it without needing spaCy's pip-based downloader (uv venvs have no pip).
spacy>=3.8
en_core_web_sm @ https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.8.0/en_core_web_sm-3.8.0-py3-none-any.whl
tqdm>=4.66.0
# Web search (no API key required) — ddgs is the maintained successor of the
# now-deprecated duckduckgo-search package (better rate-limit handling).
ddgs>=9.0.0
# Video frame sampling for Vision QA of generated video (Kling multi-shot, etc.).
# Used by src/utils/video_frames.py; degrades gracefully (OpenCV / ffmpeg-on-PATH
# fallbacks) if absent, but this is the recommended backend.
imageio[ffmpeg]>=2.34.0

# ── Image annotation: locating what to mark ─────────────────────────────────
# SAM 3 turns a text prompt into boxes + masks, so "circle the bolts" resolves
# in-process — no round-trip through a ComfyUI graph, which is what lets
# annotation still work while ComfyUI is down.
#
# This is Meta's own package, NOT the ComfyUI-SAM3 node's vendored copy: that one
# imports comfy.ops and comfy.ldm.modules.attention across six modules and cannot
# be used outside ComfyUI.
#
# It reuses the checkpoint ComfyUI already has — models/sam3/sam3.safetensors from
# the ungated apozz/sam3-safetensors mirror — so there is nothing to download and
# no need for the gated facebook/sam3 repo. Two things about that file: it is the
# unified VIDEO model, with the image model under a "detector." key prefix (strip
# it and all 1134 image-model keys are covered), and upstream's own loader is
# torch.load, i.e. a .pt pickle, so agentY reads the safetensors itself.
#
# Imported lazily: without these agentY still runs, it just loses the SAM3 locate
# backend and annotation falls back to the vision model.
sam3>=0.1.4
# Two imports sam3 does not declare. triton is a distance-transform kernel pulled
# in transitively by `import sam3` itself, and upstream triton publishes no
# Windows wheels — the community fork does, under the same `triton` module name.
psutil>=5.9.0
triton-windows>=3.7.1 ; sys_platform == "win32"
triton>=3.0.0 ; sys_platform != "win32"
# GPU torch is NOT resolvable from PyPI on Windows (the win_amd64 wheel there is
# CPU-only, ~0.12 GB), and on CPU a single grounding call goes from ~0.2s to the
# order of a minute. Install it from the CUDA index BEFORE this file, so the
# sam3 -> timm -> torch chain below finds it already satisfied:
#   .venv/Scripts/python -m pip install torch torchvision \
#       --index-url https://download.pytorch.org/whl/cu128
# Left unpinned here because the right build is machine-specific (CUDA version).
# Verified on 2.11.0+cu128 / torchvision 0.26.0+cu128, Python 3.12, RTX 5090.
#
# NOTE: sam3 constrains numpy<2.4, so installing it downgrades numpy (2.4.4 ->
# 2.3.5 here). `pip check` stays clean and every agentY module still imports.